<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Tên món ăn -->
    <TextView
        android:id="@+id/detail_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:text="Food Name"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/detail_item_img"
        android:layout_marginBottom="8dp" />

    <!-- Hình ảnh món ăn -->
    <ImageView
        android:id="@+id/detail_item_img"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/detail_item_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp" />

    <!-- Mô tả món ăn -->
    <TextView
        android:id="@+id/detail_item_description"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="This is the detail screen of the food."
        android:textSize="14sp"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/detail_item_img"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp" />

    <!-- Số lượng món ăn -->
    <EditText
        android:id="@+id/detail_item_ord_number"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:hint="Quantity"
        android:gravity="center"
        android:background="@android:drawable/edit_text"
        app:layout_constraintTop_toBottomOf="@id/detail_item_description"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp" />

    <!-- Nhóm nút hành động -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/detail_item_ord_number"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Nút Order -->
        <Button
            android:id="@+id/detail_order"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Order"
            android:backgroundTint="#80F484"
            android:textColor="@android:color/white"
            android:padding="8dp" />

        <Space
            android:layout_width="16dp"
            android:layout_height="match_parent" />

        <!-- Nút Reset -->
        <Button
            android:id="@+id/detail_reset"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Reset"
            android:backgroundTint="#80F484"
            android:textColor="@android:color/white"
            android:padding="8dp" />

    </LinearLayout>

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ERROR: C:\Users\ACER\AndroidStudioProjects\ptpm_hnue\app\src\main\res\layout\activity_detail.xml:81: AAPT: error: resource color/teal_700 (aka com.nmq.foodninjaver2:color/teal_700) not found.&#xA;"
        tools:layout_editor_absoluteX="220dp"
        tools:layout_editor_absoluteY="523dp"
        tools:ignore="MissingConstraints" />

</androidx.constraintlayout.widget.ConstraintLayout>
